DEV_LOCALHOST=172.24.0.1

CONFIG_SVC_HOST=0.0.0.0
CONFIG_SVC_PORT=8080
CONFIG_SVC=${CONFIG_SVC_HOST}:${CONFIG_SVC_PORT}

SWEEPER_HTTP_UI_PORT=9099
TAPE_HTTP_UI_PORT=9098

JAEGER_HTTP_UI_PORT=16686
JAEGER_GRPC_OTEL_PORT=4317
JAEGER_HOST=${DEV_LOCALHOST}

PROMETHEUS_HOST=${DEV_LOCALHOST}
PROMETHEUS_PORT=9090
PROMETHEUS_ADDR=${PROMETHEUS_HOST}:${PROMETHEUS_PORT}

COLLECTOR_HOST=${DEV_LOCALHOST}
COLLECTOR_OTEL=4317

NOMAD_ENDPOINT=http://${DEV_LOCALHOST}:4646
TRACE_ENDPOINT=http://${COLLECTOR_HOST}:${COLLECTOR_OTEL}
METER_ENDPOINT=http://${COLLECTOR_HOST}:${COLLECTOR_OTEL}
